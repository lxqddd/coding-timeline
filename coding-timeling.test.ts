import { expect, it, describe } from 'vitest'
import { CodingTimeline } from './src/index'

const codingTimeline = new CodingTimeline()

describe('test push', () => {
  // it('when use push, the list should add new', () )
})


// [

//   {
//   val: 'xxx',
//   add: true | undefined,
//   remove: 
//   }

// ]